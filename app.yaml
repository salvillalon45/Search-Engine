runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: /static
  static_dir: static
- url: /.*
  script: main.app

libraries:
- name: ssl
  version: latest
- name: flask
  version: latest
- name: numpy
  version: "1.6.1"

skip_files:
- WEBPAGES_RAW/
- ^(.*/)?.*/WEBPAGES_RAW/.*
